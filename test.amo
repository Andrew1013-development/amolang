#include "include.amo"
#define TEST 15

// this is only a test file

int main() {
    int a;
    int b = 16;
    int c = TEST - 1;
    int d = b + c;
    a = foo(9, 11);
    b = foo(c, d) + a - 10;
    printf("this is a test");
    return 0;
}
