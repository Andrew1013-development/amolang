void solve(int n) {
    printf(n * 3 + 2);
}
int area_rect(int width, int length) {
    return width * length;
}
string dog2(string su, string si, string trum) {
    return su + si + trum;
}
int main() {
    return 0;
}
