#include "other1.amo"
#include "other2.amo"
#include "other3.amo"

int main() {
    int a = func1(1);
    int b = func2(2,3);
    int c = func3(4,5,6);
    int d = a + b + c;
    return 0;
}